{
  "name": "kloud-web-lightroom",
  "private": true,
  "version": "0.1.0",
  "description": "KLOUD web photo editor prototype (Lightroom-style, non-destructive, WebGL2).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc -p tsconfig.node.json",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "fflate": "^0.8.3",
    "libraw-wasm": "^1.6.0",
    "utif": "^3.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.4",
    "typescript": "^5.9.3",
    "vite": "^8.3.0"
  }
}
